{
  "name": "base64k",
  "version": "1.0.1",
  "description": "Base64 encode decode with key in pure javascript/typescript.",
  "scripts": {
    "test": "ts-mocha -p tsconfig.test.json tests/**/*.test.ts",
    "build": "tsc -p tsconfig.json && rollup -c && uglifyjs dist/base64k.js -m > dist/base64k.min.js"
  },
  "author": "Herudi",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.0.2",
    "@rollup/plugin-multi-entry": "^3.0.0",
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.2",
    "chai": "^4.2.0",
    "mocha": "^7.1.1",
    "rollup": "^2.1.0",
    "ts-mocha": "^7.0.0",
    "typescript": "^3.8.3",
    "uglify-js": "^3.8.0"
  },
  "main": "lib/index.js",
  "typings": "lib/index.d.js",
  "keywords": [
    "base64 with key",
    "encode with key",
    "decode with key",
    "encrypt with key",
    "decrypt with key",
    "base64",
    "base64k"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/herudi/base64k.git"
  }
}
